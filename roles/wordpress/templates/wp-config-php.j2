<?php

// ** Database settings - You can get this info from your web host ** //
/** The name of the database for WordPress */
define( 'DB_NAME', '{{ db_name }}' );

/** Database username */
define( 'DB_USER', '{{ db_user }}' );

/** Database password */
define( 'DB_PASSWORD', '{{ db_password }}' );

/** Database hostname */
define( 'DB_HOST', '{{ db_host }}' );

/** Database charset to use in creating database tables. */
define( 'DB_CHARSET', 'utf8mb4' );

/** The database collate type. Don't change this if in doubt. */
define( 'DB_COLLATE', '' );

define( 'MYSQL_CLIENT_FLAGS', MYSQLI_CLIENT_SSL );

// Memory and performance settings
define( 'WP_MEMORY_LIMIT', '256M' );
define( 'WP_MAX_MEMORY_LIMIT', '512M' );
define( 'AUTOSAVE_INTERVAL', 86400 );
define( 'WP_POST_REVISIONS', 3 );
define( 'EMPTY_TRASH_DAYS', 7 );
define( 'WP_CRON_LOCK_TIMEOUT', 60 );

// Security settings
// define( 'FORCE_SSL_ADMIN', true );

// Redis connection settings
define( 'WP_REDIS_SCHEME', 'unix' );  // Use Unix socket
define( 'WP_REDIS_PATH', '/var/run/redis/redis.sock' );  // Unix socket path

// If using TCP instead of Unix socket, use these settings:
// define( 'WP_REDIS_HOST', 'localhost' );
// define( 'WP_REDIS_PORT', 6379 );

// Redis password (if applicable)
// define( 'WP_REDIS_PASSWORD', 'your_redis_password' );

// Prefix and database settings
define( 'WP_REDIS_PREFIX', '{{ db_name }}' );  // Use database name as prefix
// define( 'WP_REDIS_DATABASE', 0 );  // Redis database number to use (0-15)

// Timeout settings
// define( 'WP_REDIS_TIMEOUT', 1 );  // Connection timeout in seconds
// define( 'WP_REDIS_READ_TIMEOUT', 1 );  // Read timeout in seconds

// Cache settings
define( 'WP_CACHE', true );  // Enable object cache
define( 'WP_CACHE_KEY_SALT', '{{ db_name }}' );  // Salt for cache keys
define( 'WP_REDIS_SELECTIVE_FLUSH', true );  // Enable selective flush
define( 'WP_REDIS_MAXTTL', 86400 );  // Maximum cache lifetime (1 day)

// Advanced settings
// define( 'WP_REDIS_IGBINARY', true );  // Use igbinary serialization if available
// define( 'WP_REDIS_COMPRESSION', 'zstd' );  // Use zstd compression if available

// Reconnection settings
// define( 'WP_REDIS_RETRY_INTERVAL', 100 );  // Retry interval in milliseconds
// define( 'WP_REDIS_MAX_RETRIES', 3 );  // Maximum number of retries

// Debug settings
// define( 'WP_REDIS_DISABLED', false );  // Set to true to disable Redis (for debugging)

// Logging settings
// define( 'WP_REDIS_TRACE_ALLOWED_COMMANDS', ['get', 'set', 'lpush'] );
// define( 'WP_REDIS_LOG_SEVERITY', 1 );  // 0-3, 0 being most verbose

// The following are advanced configurations, uncomment as needed

// Sharding settings
// define( 'WP_REDIS_SHARDS', ['tcp://127.0.0.1:6379', 'tcp://127.0.0.1:6380'] );

// Replication settings
// define( 'WP_REDIS_SERVERS', [
//     'tcp://127.0.0.1:6379?alias=master',
//     'tcp://127.0.0.1:6380?alias=slave1',
// ] );

// Cluster settings
// define( 'WP_REDIS_CLUSTER', ['127.0.0.1:6379', '127.0.0.1:6380'] );

/**#@+
 * Authentication unique keys and salts.
 *
 * Change these to different unique phrases! You can generate these using
 * the {@link https://api.wordpress.org/secret-key/1.1/salt/ WordPress.org secret-key service}.
 *
 * You can change these at any point in time to invalidate all existing cookies.
 * This will force all users to have to log in again.
 *
 * @since 2.6.0
 */
define( 'AUTH_KEY',         'put your unique phrase here' );
define( 'SECURE_AUTH_KEY',  'put your unique phrase here' );
define( 'LOGGED_IN_KEY',    'put your unique phrase here' );
define( 'NONCE_KEY',        'put your unique phrase here' );
define( 'AUTH_SALT',        'put your unique phrase here' );
define( 'SECURE_AUTH_SALT', 'put your unique phrase here' );
define( 'LOGGED_IN_SALT',   'put your unique phrase here' );
define( 'NONCE_SALT',       'put your unique phrase here' );

/**#@-*/

/**
 * WordPress database table prefix.
 *
 * You can have multiple installations in one database if you give each
 * a unique prefix. Only numbers, letters, and underscores please!
 */
$table_prefix = 'wp_';

/**
 * For developers: WordPress debugging mode.
 *
 * Change this to true to enable the display of notices during development.
 * It is strongly recommended that plugin and theme developers use WP_DEBUG
 * in their development environments.
 *
 * For information on other constants that can be used for debugging,
 * visit the documentation.
 *
 * @link https://developer.wordpress.org/advanced-administration/debug/debug-wordpress/
 */
define( 'WP_DEBUG', false );
define( 'WP_DEBUG_LOG', false );
define( 'WP_DEBUG_DISPLAY', false );
define( 'SCRIPT_DEBUG', false );
define( 'SAVEQUERIES', false );

/* Add any custom values between this line and the "stop editing" line. */

/* That's all, stop editing! Happy publishing. */

/** Absolute path to the WordPress directory. */
if ( ! defined( 'ABSPATH' ) ) {
    define( 'ABSPATH', __DIR__ . '/' );
}

/** Sets up WordPress vars and included files. */
require_once ABSPATH . 'wp-settings.php';
